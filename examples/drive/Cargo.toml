[package]
name = "drive"
version = "0.1.0"
edition = "2024"

[dependencies]
bytemuck = { version = "1", features = ["derive"] }
pm-world = { path = "../../crates/pm-world" }
pm-world-sdl = { path = "../../crates/pm-world-sdl" }
